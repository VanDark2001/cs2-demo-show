package com.csdemo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** CS2 Demo Show 后端启动入口，负责初始化 Spring 容器和 Web 服务。 */
@SpringBootApplication
public class DemoShowApplication { public static void main(String[] args) { SpringApplication.run(DemoShowApplication.class, args); } }
