# 统计口径（不得臆想）

本文件记录用户已经明确确认的业务规则。代码或查询修改后必须用 MySQL 中的真实 Demo 数据验证。

## 1. 正式比赛边界

- 优先使用 `matches.official_start_tick`，其次使用 `matches.warmup_end_tick`。
- 解析器把 `events.is_warmup` 写入事件表；正式数据不得包含热身事件。
- 统计不能包含最后一个正式回合结束后玩家退出服务器产生的死亡。
- 当前后端还通过“该事件之后最近的有效 `round_end` 的 winner 必须是 `T` 或 `CT`”过滤无效事件。
- 每秒 64 tick，只用于把 tick 转为时间；不能把 tick 当回合数或直接参与伤害统计。

## 2. 队伍

- 唯一分组依据是同一场比赛的 `match_players.team_number`。
- 相同 `team_number` 必须属于同一队，不允许根据 T/CT 阵营或排序重新混组。
- 前端只显示 A组、B组，不显示原始 team number 和“team_number: 2 · 5人”等文字。
- 正常比赛应是两组各 5 人；如果不是，先检查解析后的 `match_players`，不要在前端硬切前五/后五。
- 部分 12:12 Demo 的 `parse_player_info()` 会返回空表；解析器应从带 `team_num` 的正式玩家事件恢复阵容并写入 `match_players`，仍不得由前端猜测队伍。

## 3. K / D / A

- 击杀：正式有效回合内，攻击者和受害者属于不同 `team_number`，且不是自杀。
- 死亡：正式有效回合内的受害事件；排除赛后退出事件。
- 助攻：从 kill 的 `data_json.assister_steamid` 读取，并确认助攻者和受害者不是同队。
- 前端列顺序当前是 `K / A / D`，但展示值必须与标签顺序一致；修改 UI 时不要误用 `kda` 字符串。

## 4. ADR

- ADR = 正式有效伤害总和 / 正式有效回合数。
- 友军伤害不计入。
- `inferno`（燃烧弹）伤害需要计入。
- 每次伤害不向下取整，使用解析器记录的实际生命伤害。
- 同一个回合内，同一个受害者最多贡献 100 点有效生命伤害。
- 如果一个受害者先后被多个玩家造成伤害，所有攻击者对该受害者的累计有效伤害仍不得超过 100；后续攻击者只能取得剩余生命值部分。
- 上限键应为“回合 + victim_steamid”，事件按 tick/id 顺序累计。
- ADR 分母必须是正式比赛回合数，不能用分钟、tick 数或玩家存活回合数。

## 5. 比分、换边与颜色

- 常规赛先到 13 分获胜；12:12 后进入 MR3 加时，加时半场每 3 回合换边。
- 常规赛第 12 回合后换边；进入加时后在第 27、30、33……回合后继续换边。
- `match_players.team_number` 是 Demo 结束时的队伍分组，因此历史回合胜方必须按从
  该回合到最终回合之间的换边次数映射回最终队伍，不能只写死一次半场换边。
- 达到 13 且为胜方的比分显示绿色；输方比分显示红色。
- 比分来自正式 `round_end` 胜方累计，并正确处理换边；不能用击杀总数推算。

## 6. 武器

- 武器名后缀 `_txz数字` 必须合并到基础武器，例如 `ak47_txz12 -> ak47`、`m4a1_txz12 -> m4a1`。
- 武器名后缀 `_vip` 同样合并到基础武器；聚合必须在后端完成，不能只把重复行显示成相同名称。
- `inferno` 是燃烧弹造成的伤害/击杀，前端显示“燃烧弹”，避免与地图 Inferno 混淆。
- 所有刀具变体统一聚合为 `knife`，包括 `knife_m9_bayonet`、其他 `knife_*` 以及 `bayonet`。

## 7. 高光与残局

- 高光：同一正式回合内至少 3 杀。
- 残局：玩家在 1vX 状态下且最终赢下该回合。
- 多杀不等于残局，两类数据必须独立计算和展示。

## 8. Rating

当前后端使用近似 Rating 3.0 的自定义加权公式，并非 HLTV 官方公式：

```text
impact = max(0, 2.13*KPR + 0.42*APR - 0.41)
rating = 0.32*(KPR/0.68)
       + 0.18*(survival/0.65)
       + 0.24*(ADR/70)
       + 0.10*(APR/0.16)
       + 0.10*(impact/1.10)
       + 0.06*(multiKillRoundRate/0.20)
```

最终限制在 `0.00–2.50`。如果修改算法，要明确说明这是近似值并用真实比赛回归测试。

## 9. 时间

- 网页比赛时间优先读取 `matches.recorded_at`，而不是导入时间。
- 当前解析器使用 Demo 文件最后修改时间，`recording_time_source='file_last_modified'`。
- 若以后能从 Demo header 获取真实录制时间，应更新来源字段，不要悄悄回退到 `imported_at`。
## 10. WE（自定义全回合贡献分）

- WE 是绝对贡献分，不是官方 RWS；不再让玩家按队内占比瓜分固定分数，避免强队核心因队友整体表现好而被压低。
- `WE = 4 × ADR / 100 + 3 × KPR + APR + 2 × 本队正式回合胜率`。
- KPR、APR 和本队正式回合胜率的分母均为全部正式有效回合数；失败回合的击杀、助攻和伤害仍正常计入。
- WE 限制在 0.0–16.0，保留一位小数。
- 队伍使用 `match_players.team_number`；正式边界、敌我判断、友军过滤、每回合受害者累计伤害上限 100 与 ADR 一致。
